enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {paypal, googlePlay, applePay, visa, masterCard, creditCard, payStack, razorPay, paytm}